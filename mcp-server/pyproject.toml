[project]
name = "claude-approval-device-mcp"
version = "0.1.0"
description = "MCP shim that forwards Claude Code commands to the Claude Approver menu-bar app via localhost HTTP"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.0.0",
    "httpx>=0.27.0",
]
